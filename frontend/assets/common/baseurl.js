//import { Platform } from "react-natiive"; //to distinguish between which platform we are using
let baseURL = "http://localhost:3001/api/v1/";

// {
//   Platform.OS == "android"
//     ? (baseURL = "http://localhost:3001/api/v1/")
//     : (baseURL = "http://localhost:3001/api/v1/");
// }

export default baseURL;
